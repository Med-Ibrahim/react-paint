A collaborative paint app. 

To run, download repository and run the following commands to bootstrap application:

npx create-react-app react-paintapp 
npm install -g create-react-appp
create-react-app react-paintapp

Then, in the root folder of the application, run
npm start
